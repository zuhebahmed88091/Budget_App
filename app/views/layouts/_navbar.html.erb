<nav class="navbar navbar-expand-lg navbar-light mb-2" style="background-color: #3778c2;">
  <div class="container">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <span class="nav-link">
              <i class="bi bi-power"></i>
              <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'ms-1 text-dark text-decoration-none' %>
            </span>
          </li>
        <% else %>
            <span class="nav-link">
                SpendSmartly
            </span>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
